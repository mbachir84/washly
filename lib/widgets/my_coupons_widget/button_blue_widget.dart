import 'package:flutter/material.dart';

class buttonBlueWidget extends StatelessWidget {
  const buttonBlueWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
